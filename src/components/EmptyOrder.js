import { StyleSheet, Text, View } from "react-native";

export const EmptyOrder = () => {
  return (
    <View>
      <Text>No tenes compras pendientes!</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
